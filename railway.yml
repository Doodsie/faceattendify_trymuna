build:
  name: python-build
  script: pip install -r requirements.txt

start:
  name: python-start
  script: gunicorn main:app -b :5000 --timeout 120 --workers=4 --threads=3 --worker-connections=1000
